{
  "name": "xpens",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "now-build": "next build && next export -o dist",
    "deploy": "now && now alias && now rm xpens --safe --yes",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "evergreen-ui": "^4.11.0",
    "next": "^13.1.1",
    "react": "^16.7.0",
    "react-dom": "^16.7.0"
  }
}
